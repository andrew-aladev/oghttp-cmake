set (NAME "HEADER_NAME_1_1")

# include (GenerateConstantsFromRegistryWrapper)
# generate_constants_from_registry_wrapper (
#   ${NAME}
#   "http-status-codes" "${PROJECT_SOURCE_DIR}/registry/http-status-codes.xml"
#   "http-warn-codes" "${PROJECT_SOURCE_DIR}/registry/http-warn-codes.xml"
# )

# include (GenerateConstantsProcessorWrapper)
# generate_constants_processor_wrapper (${NAME})
